version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p public
        - cp src/index.html public/index.html
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
